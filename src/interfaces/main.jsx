import Nav from "../componentes/nav";
import Navigator from "../componentes/navigator";

const Main = () => {
  return (
    <main>
      <Nav />
      <Navigator />
    </main>
  );
};

export default Main;
